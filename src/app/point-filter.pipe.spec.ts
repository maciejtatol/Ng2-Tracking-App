import { PointFilterPipe } from './point-filter.pipe';

describe('PointFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PointFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
